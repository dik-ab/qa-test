/**
 * 質問拡張データ生成用のプロンプト
 */
export const EXPANSION_DATA_PROMPT = `あなたはFAQ検索用の質問拡張データを生成するアシスタントです。
入力として「FAQ質問（Q）」と「FAQ回答（A）」が与えられます。
出力は [言い換え] と [ユーザーの状況/悩み] の2ブロックのみ返してください。

# 出力ルール
- [言い換え] は FAQ質問と同じ意味を持つ自然な疑問文を2~3行生成する。
- [ユーザーの状況/悩み] は FAQ質問に関連してユーザーが直面しそうな具体的な状況や課題を2〜3行生成する。
- 各FAQの拡張文は合計4~6行程度に抑える（多すぎないこと）。
- 各行は短い自然文にする（1行＝1意図）。複数要素を詰め込まない。
- [ユーザーの状況/悩み] では疑問文は禁止。平叙文で「状態・事実」を簡潔に書く。前提条件と課題が強く結びつく場合は、短文に分けずに1文にまとめて記述してよい。
- 感情的なストーリーや動機は禁止。
- 「困っている」「不安」などの感情語も禁止。必ず「利用できない」「不明」「確認したい」といった客観的な状態表現に置き換える。
- 固有名詞は必要な範囲で使用し、繰り返しすぎない。比較を問う文のみ両方を同じ行に出す。それ以外は片方ずつ記述する。
- Q/Aにない新しい事実は作らない。URLや長い手順は含めない。
- 否定語（例: 使えない、できない）はトラブルFAQにのみ使用し、通常FAQでは条件や方法を表す表現に置き換える。
- 出力は以下のフォーマットに厳密に従う。前置きや解説は禁止。

FAQ質問（Q）: \${question}
FAQ回答（A）: \${answer}

# 出力フォーマット例
[言い換え]
文1
文2
文3

[ユーザーの状況/悩み]
文1
文2
`;

/**
 * ページ数と場所を抽出するプロンプト
 */
export const PAGE_LOCATION_PROMPT = `以下の質問と回答から、該当する情報が記載されているページ数と場所（セクション名など）を抽出してください。

質問: \${question}
回答: \${answer}

ドキュメント全文:
\${text}

## 出力形式
以下のJSON形式で返してください：
{
  "pageNumber": "ページ数（例: 3, 5-7, 不明）",
  "location": "場所・セクション名（例: 第2章 申込方法, キャンペーン概要, 不明）"
}

注意事項:
- ページ数は具体的な数字で記載（複数ページにまたがる場合は「3-5」のような形式）
- 場所はドキュメント内のセクション名や章のタイトルを使用
- 情報が見つからない場合は「不明」と記載
`;