import { BedrockRuntimeClient, InvokeModelCommand } from '@aws-sdk/client-bedrock-runtime';
import { config } from 'dotenv';

// 開発環境でのみdotenvを読み込む
if (process.env.NODE_ENV !== 'production') {
  config();
}

// AWS Bedrock クライアントの初期化
const bedrockClient = new BedrockRuntimeClient({
  region: 'us-east-1',
  credentials: {
    accessKeyId: process.env.AWS_ACCESS_KEY_ID || '',
    secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY || '',
  },
});

// Claude 3.5 Sonnet モデルID
const MODEL_ID = 'us.anthropic.claude-3-7-sonnet-20250219-v1:0';

// デフォルトプロンプトを定数として定義
export const DEFAULT_PROMPT = `
    <document>
    \${text}
    </document>

    上記の文書を分析して、以下の条件に従って\${numQuestions}個の質問と回答を生成してください：

    ## 【重要】固有名詞の保持について：
    - **キャンペーン名、サービス名、商品名は必ず完全な正式名称を使用する**
    - 「dポイント（期間・用途限定）最大全額還元キャンペーン」「dアカウント新規ID連携で最大10,000ポイントプレゼント」「対象予約利用額の最大全額ポイント還元」などの名称は一切省略しない
    - 「このキャンペーン」「該当キャンペーン」「当キャンペーン」などの代名詞は絶対に使用しない
    - 文書内に記載されている正式名称をそのまま質問文に含める

    ## 質問生成の条件：
    1. **完全な固有名詞の使用**：キャンペーン名、サービス名、商品名は文書に記載されている完全な正式名称を必ず使用し、一切省略しない
    2. **具体性を重視**：「このキャンペーン」「この制度」「この手順」などの曖昧な表現は絶対に避け、文書内の具体的な名称、数値、期間、条件を明記した質問を作成する
    3. **理解が困難な箇所を重点的に**：専門用語、複雑な概念、手順、重要な数値や日付、条件や制限事項など、ユーザーが理解に困る可能性が高い部分を優先的に質問化する
    4. **実用的な詳細情報**：ユーザーが実際に行動を起こす際に必要となる具体的な情報（金額、期間、手順、対象者、条件など）を質問に含める
    5. **多様な質問パターン**：「〜の金額は」「〜の期限は」「〜の対象者は」「〜の手順は」「〜の条件は」「〜の計算方法は」など、様々な角度からの質問を含める
    6. **文脈を含めた質問**：ユーザーが「あれ」「それ」「この部分」などの曖昧な表現を使っても理解できるよう、文書の具体的な内容を質問文に含める

    ## 回答生成の条件：
    1. **簡潔性**：要点を絞り、冗長な説明は避ける（目安：1-3文程度）
    2. **丁寧さ**：敬語を使用し、分かりやすい言葉で説明する
    3. **正確性**：文書の内容に忠実で、推測や憶測は含めない
    4. **具体的な数値や条件**：金額、期間、パーセンテージ、対象者などの具体的な情報を明記する
    5. **構造化**：必要に応じて箇条書きや番号付きリストを使用して読みやすくする
    6. **文脈提供**：回答だけでなく、なぜそうなのかの背景も簡潔に含める

    ## 質問の具体性の例：
    ❌ 悪い例：「このキャンペーンで得られるポイントは何ですか？」
    ❌ 悪い例：「キャンペーンで得られるポイントは何ですか？」
    ✅ 良い例：「dアカウント新規ID連携で最大10,000ポイントプレゼントキャンペーンで得られるdポイントの最大金額は何ポイントですか？」

    ❌ 悪い例：「キャンペーンの期限はいつですか？」
    ❌ 悪い例：「エントリー期間はいつですか？」
    ✅ 良い例：「dポイント（期間・用途限定）最大全額還元キャンペーンのエントリー期間はいつからいつまでですか？」

    ❌ 悪い例：「対象者は誰ですか？」
    ❌ 悪い例：「当選人数は何人ですか？」
    ✅ 良い例：「dアカウント新規ID連携で最大10,000ポイントプレゼントキャンペーンの1等の当選人数と獲得ポイント数を教えてください。」

    ❌ 悪い例：「宿泊期間はいつですか？」
    ✅ 良い例：「対象予約利用額の最大全額ポイント還元キャンペーンの宿泊・利用対象期間はいつからいつまでですか？」

    ## 出力形式：
    以下のJSON形式で返してください。JSONのみを返し、説明や追加のテキストは不要です：

    [
      {
        "question": "文書の具体的な内容を含む明確で詳細な質問文（固有名詞は完全な正式名称を使用）",
        "answer": "具体的な数値や条件を含む簡潔で丁寧な回答文"
      },
      {
        "question": "次の具体的な質問文（固有名詞は完全な正式名称を使用）",
        "answer": "次の具体的な回答文"
      }
    ]
    `;

/**
 * テキストからクエスチョンデータを生成する
 * @param text PDFから抽出したテキスト
 * @param numQuestions 生成する質問の数
 * @param customPrompt カスタムプロンプト（オプション）
 * @param comprehensiveMode 網羅的生成モード（オプション）
 * @returns 生成された質問と回答のリスト
 */
export async function generateQuestionsFromText(
  text: string,
  numQuestions: number = 5,
  customPrompt?: string,
  comprehensiveMode: boolean = false
): Promise<Array<{ question: string, answer: string }>> {
  try {
    // カスタムプロンプトまたはデフォルトプロンプトを使用
    let prompt: string;
    
    if (customPrompt) {
      // カスタムプロンプトの変数を置換
      prompt = customPrompt
        .replace(/\$\{text\}/g, text)
        .replace(/\$\{numQuestions\}/g, numQuestions.toString());
    } else if (comprehensiveMode) {
      // 網羅的生成モード用のプロンプト
      prompt = `
    <document>
    ${text}
    </document>

    上記の文書を詳細に分析して、ユーザーが疑問に思うであろう箇所を網羅的に特定し、細かい部分まで質問と回答を生成してください。
    指定された質問数（${numQuestions}個）は参考値として、文書の内容に応じてより多くの質問を生成しても構いません。

    ## 【重要】網羅的分析の観点：
    1. **基本情報の詳細化**：名称、期間、金額、対象者、条件などの基本情報を細分化して質問化
    2. **手続きの各ステップ**：申込み、エントリー、利用、受取りなど各段階での詳細な手順
    3. **制限事項と注意点**：利用制限、対象外条件、注意事項、禁止事項など
    4. **計算方法と仕組み**：ポイント計算、還元率、割引計算などの詳細
    5. **例外ケースと特殊条件**：特別な条件、例外的な取扱い、イレギュラーケース
    6. **関連サービスとの連携**：他のサービスとの関係、併用可否、相互作用
    7. **トラブルシューティング**：よくある問題、解決方法、問い合わせ先
    8. **時系列での変化**：期間による条件変更、段階的な特典、時限的な内容

    ## 【重要】固有名詞の保持について：
    - **キャンペーン名、サービス名、商品名は必ず完全な正式名称を使用する**
    - 「このキャンペーン」「該当キャンペーン」「当キャンペーン」などの代名詞は絶対に使用しない
    - 文書内に記載されている正式名称をそのまま質問文に含める

    ## 質問生成の条件：
    1. **完全な固有名詞の使用**：キャンペーン名、サービス名、商品名は文書に記載されている完全な正式名称を必ず使用し、一切省略しない
    2. **具体性を重視**：「このキャンペーン」「この制度」「この手順」などの曖昧な表現は絶対に避け、文書内の具体的な名称、数値、期間、条件を明記した質問を作成する
    3. **細分化された質問**：大きなトピックを小さな要素に分解し、それぞれについて詳細な質問を作成
    4. **実用的な詳細情報**：ユーザーが実際に行動を起こす際に必要となる具体的な情報を網羅
    5. **多様な質問パターン**：「〜の金額は」「〜の期限は」「〜の対象者は」「〜の手順は」「〜の条件は」「〜の計算方法は」「〜の場合はどうなるか」など
    6. **文脈を含めた質問**：ユーザーが「あれ」「それ」「この部分」などの曖昧な表現を使っても理解できるよう、文書の具体的な内容を質問文に含める
    7. **段階的な詳細度**：基本的な質問から詳細な質問まで、様々なレベルの疑問を網羅

    ## 回答生成の条件：
    1. **簡潔性**：要点を絞り、冗長な説明は避ける（目安：1-3文程度）
    2. **丁寧さ**：敬語を使用し、分かりやすい言葉で説明する
    3. **正確性**：文書の内容に忠実で、推測や憶測は含めない
    4. **具体的な数値や条件**：金額、期間、パーセンテージ、対象者などの具体的な情報を明記する
    5. **構造化**：必要に応じて箇条書きや番号付きリストを使用して読みやすくする
    6. **文脈提供**：回答だけでなく、なぜそうなのかの背景も簡潔に含める

    ## 出力形式：
    以下のJSON形式で返してください。JSONのみを返し、説明や追加のテキストは不要です：

    [
      {
        "question": "文書の具体的な内容を含む明確で詳細な質問文（固有名詞は完全な正式名称を使用）",
        "answer": "具体的な数値や条件を含む簡潔で丁寧な回答文"
      },
      {
        "question": "次の具体的な質問文（固有名詞は完全な正式名称を使用）",
        "answer": "次の具体的な回答文"
      }
    ]
    `;
    } else {
      // デフォルトプロンプトを使用
      prompt = `
    <document>
    ${text}
    </document>

    上記の文書を分析して、以下の条件に従って${numQuestions}個の質問と回答を生成してください：

    ## 【重要】固有名詞の保持について：
    - **キャンペーン名、サービス名、商品名は必ず完全な正式名称を使用する**
    - 「dポイント（期間・用途限定）最大全額還元キャンペーン」「dアカウント新規ID連携で最大10,000ポイントプレゼント」「対象予約利用額の最大全額ポイント還元」などの名称は一切省略しない
    - 「このキャンペーン」「該当キャンペーン」「当キャンペーン」などの代名詞は絶対に使用しない
    - 文書内に記載されている正式名称をそのまま質問文に含める

    ## 質問生成の条件：
    1. **完全な固有名詞の使用**：キャンペーン名、サービス名、商品名は文書に記載されている完全な正式名称を必ず使用し、一切省略しない
    2. **具体性を重視**：「このキャンペーン」「この制度」「この手順」などの曖昧な表現は絶対に避け、文書内の具体的な名称、数値、期間、条件を明記した質問を作成する
    3. **理解が困難な箇所を重点的に**：専門用語、複雑な概念、手順、重要な数値や日付、条件や制限事項など、ユーザーが理解に困る可能性が高い部分を優先的に質問化する
    4. **実用的な詳細情報**：ユーザーが実際に行動を起こす際に必要となる具体的な情報（金額、期間、手順、対象者、条件など）を質問に含める
    5. **多様な質問パターン**：「〜の金額は」「〜の期限は」「〜の対象者は」「〜の手順は」「〜の条件は」「〜の計算方法は」など、様々な角度からの質問を含める
    6. **文脈を含めた質問**：ユーザーが「あれ」「それ」「この部分」などの曖昧な表現を使っても理解できるよう、文書の具体的な内容を質問文に含める

    ## 回答生成の条件：
    1. **簡潔性**：要点を絞り、冗長な説明は避ける（目安：1-3文程度）
    2. **丁寧さ**：敬語を使用し、分かりやすい言葉で説明する
    3. **正確性**：文書の内容に忠実で、推測や憶測は含めない
    4. **具体的な数値や条件**：金額、期間、パーセンテージ、対象者などの具体的な情報を明記する
    5. **構造化**：必要に応じて箇条書きや番号付きリストを使用して読みやすくする
    6. **文脈提供**：回答だけでなく、なぜそうなのかの背景も簡潔に含める

    ## 質問の具体性の例：
    ❌ 悪い例：「このキャンペーンで得られるポイントは何ですか？」
    ❌ 悪い例：「キャンペーンで得られるポイントは何ですか？」
    ✅ 良い例：「dアカウント新規ID連携で最大10,000ポイントプレゼントキャンペーンで得られるdポイントの最大金額は何ポイントですか？」

    ❌ 悪い例：「キャンペーンの期限はいつですか？」
    ❌ 悪い例：「エントリー期間はいつですか？」
    ✅ 良い例：「dポイント（期間・用途限定）最大全額還元キャンペーンのエントリー期間はいつからいつまでですか？」

    ❌ 悪い例：「対象者は誰ですか？」
    ❌ 悪い例：「当選人数は何人ですか？」
    ✅ 良い例：「dアカウント新規ID連携で最大10,000ポイントプレゼントキャンペーンの1等の当選人数と獲得ポイント数を教えてください。」

    ❌ 悪い例：「宿泊期間はいつですか？」
    ✅ 良い例：「対象予約利用額の最大全額ポイント還元キャンペーンの宿泊・利用対象期間はいつからいつまでですか？」

    ## 出力形式：
    以下のJSON形式で返してください。JSONのみを返し、説明や追加のテキストは不要です：

    [
      {
        "question": "文書の具体的な内容を含む明確で詳細な質問文（固有名詞は完全な正式名称を使用）",
        "answer": "具体的な数値や条件を含む簡潔で丁寧な回答文"
      },
      {
        "question": "次の具体的な質問文（固有名詞は完全な正式名称を使用）",
        "answer": "次の具体的な回答文"
      }
    ]
    `;
    }

    // Bedrockへのリクエスト
    const response = await bedrockClient.send(
      new InvokeModelCommand({
        modelId: MODEL_ID,
        contentType: 'application/json',
        accept: 'application/json',
        body: JSON.stringify({
          anthropic_version: 'bedrock-2023-05-31',
          max_tokens: 4096,
          messages: [
            {
              role: 'user',
              content: [
                {
                  type: 'text',
                  text: prompt
                }
              ]
            }
          ]
        }),
      })
    );
    
    // レスポンスの解析
    const responseBody = JSON.parse(new TextDecoder().decode(response.body));
    const responseText = responseBody.content[0].text;
    
    // JSONの抽出（余分なテキストがある場合に対応）
    const jsonMatch = responseText.match(/\[[\s\S]*\]/);
    if (jsonMatch) {
      return JSON.parse(jsonMatch[0]);
    }
    
    // 直接JSONとして解析できる場合
    try {
      return JSON.parse(responseText);
    } catch (error) {
      console.error('Failed to parse JSON response:', error);
      throw new Error('AIからの応答をJSONとして解析できませんでした');
    }
  } catch (error) {
    console.error('Error generating questions:', error);
    throw error;
  }
}
